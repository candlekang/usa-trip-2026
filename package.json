{
  "name": "usa-trip-2026",
  "private": true,
  "type": "module",
  "scripts": {
    "test:rules": "firebase emulators:exec --only firestore \"node --test tests/*.test.mjs\"",
    "emulators": "firebase emulators:start",
    "seed": "node scripts/seed.mjs"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.10.0",
    "firebase-admin": "^13.4.0"
  }
}
